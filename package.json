{
  "name": "@lens.kamdem/agentic-coding-plugin",
  "version": "1.0.0",
  "description": "Local, autonomous coding assistant framework built for OpenCode.",
  "main": "index.js",
  "bin": {
    "agentic-coding-plugin": "src/cli/install.js"
  },
  "scripts": {
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "format": "prettier --write src/**/*.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "verify": "node src/tools/core/verify-setup.js"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0"
  }
}
